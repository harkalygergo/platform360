<!DOCTYPE html>
<html dir="ltr" class="h-100" lang="{% if app.request.get('_locale') is not empty %}{{ app.request.get('_locale') }}{% else %}en{% endif %}" data-bs-theme="light">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="robots" content="noindex,nofollow">
        <meta name="description" content="Platform Business Management System">
        <meta name="keywords" content="platform, business, management, system, bms, crm, cms, erp, ecom, analytics, tools">
        <link rel="icon" href="/favicon.jpg" type="image/png">
        <title>{% block title %}PLATFORM V2{% endblock %}</title>

        <!--link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&display=swap" rel="stylesheet"-->

        <link rel="stylesheet" type="text/css" href="https://cdn.brandcomstudio.com/vendor/twbs/bootstrap/dist/css/bootstrap.min.css">
        <link rel="stylesheet" type="text/css" href="https://cdn.brandcomstudio.com/vendor/twbs/bootstrap-icons/font/bootstrap-icons.min.css">

        <style>
            body {
                font-family: Arial, Helvetica, sans-serif;
                font-optical-sizing: auto;
                font-style: normal;
                letter-spacing: 0.1px;
            }

            main {
                font-family: "Arial", sans-serif;
            }

            label.required:after {
                content: " * ";
                color: red;
            }
            .form-control:invalid:not(textarea) {
                border-color: #fbb0b0;
            }
            .form-control:valid:not(textarea) {
                border-color: green;
            }

            /* ADMIN */
            /*
            nav {
                max-width: 5rem;
            }
            */
            small {
                font-size:10px;
            }

        </style>

        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body class="d-flex h-100">
        {% block body %}{% endblock %}

        <script src="https://cdn.brandcomstudio.com/vendor/twbs/bootstrap/dist/js/bootstrap.min.js"></script>

    </body>
</html>
